node_modules/
.data/
.certs/
*.pem

# Environment & secrets
.env
.env.*
!.env.example

# Built/obfuscated files — generated by `npm run build`, not committed
public/*.min.js

# Product docs (strategy, roadmap, pricing — keep off both repos until reviewed)
doc/

# OS
.DS_Store
Thumbs.db
